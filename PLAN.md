# B1-1 포트폴리오 웹사이트 수행 기획서

작성일: 2026-09-10<br>
요구사항 기준: [subject.md](subject.md)

> 구현 범위 변경: 사용자의 요청으로 `.env`를 읽는 로컬 Node 서버와 GitHub 조회를 추가하고 실제 토큰 인증을 확인했다. GitHub 공개 프로필 `hkk / hkk-cody`와 README를 바탕으로 소개와 대표 프로젝트 6개를 반영했다. 브라우저는 토큰을 보관하지 않으며 GitHub Pages에서는 인증 없이 조회한다. 데스크톱·모바일·다크 모드 검증 화면을 저장했다. 공개 배포와 배포 환경 검증은 남아 있다. 실행 방법과 최종 구조는 README를 따른다.

## 1. 목표와 수행 원칙

순수 HTML, CSS, JavaScript로 자기소개와 GitHub 프로젝트를 보여주는 반응형 단일 페이지를 만들고 GitHub Pages에 배포한다. 방문자는 소개와 기술 스택을 읽고, 실제 저장소를 확인하고, 문의 폼을 작성할 수 있다.

이번 과제에서는 기능을 직접 구현하며 **사용자 이벤트 → 상태 변경 → DOM 업데이트** 흐름을 이해하는 것을 우선한다. 각 기능의 상태와 화면 갱신 함수를 구분하여, 완성 후 코드의 동작을 스스로 설명할 수 있도록 한다.

- React, Vue, jQuery, Bootstrap, Tailwind 등 외부 라이브러리는 사용하지 않는다.
- HTML은 구조, CSS는 표현, JavaScript는 상태와 동작을 담당한다.
- `const`와 `let`, `addEventListener`를 사용하고 `var`, HTML의 `onclick`, 인라인 스타일은 사용하지 않는다.
- 모바일 화면부터 완성한 뒤 태블릿과 데스크톱으로 확장한다.
- 필수 기능과 검증을 먼저 완료하고 보너스 기능은 이후 판단한다.

## 2. 구현 전 준비할 콘텐츠

아래 정보는 현재 요구사항 문서에 없으므로 구현 시 확정한다. 준비 전에는 명확한 임시 문구를 사용하고, 배포 전 실제 정보로 교체한다.

| 준비 항목 | 사용 위치 | 준비 기준 |
| --- | --- | --- |
| 이름 또는 활동명, 한 줄 소개 | Hero, 페이지 제목, Footer | 공개할 수 있는 정보 |
| 자기소개, 관심 분야, 학습 방향 | About | 짧은 문단 2~3개 |
| 프로필 이미지 | About | 사용 권한이 있는 이미지와 설명용 alt |
| 기술 스택 | Skills | 실제로 학습하거나 사용한 기술 |
| GitHub 사용자명 | Projects, Footer | 본인의 공개 저장소를 조회할 계정 |
| 공개할 소셜 링크 | Footer | 실제 사용하는 링크만 포함 |
| GitHub 저장소와 배포 URL | README | 배포 후 실제 접속 주소 기입 |

## 3. 화면 구성과 디자인 방향

읽기 쉬운 개발자 포트폴리오를 만든다. 배경색, 본문색, 강조색을 CSS 변수로 관리하고, 여백과 카드 스타일을 통일한다. 폰트는 시스템 글꼴을 기본으로 사용한다.

| 영역 | 주요 내용 | 사용자 행동 및 구현 방식 |
| --- | --- | --- |
| Header / Nav | 활동명, 섹션 링크, 테마 버튼, 모바일 메뉴 버튼 | 각 섹션으로 이동, 메뉴와 테마 전환. Flexbox 배치 |
| Hero | 인사말, 이름, 한 줄 소개 | ‘프로젝트 보기’, ‘연락하기’ CTA로 섹션 이동 |
| About | 프로필 이미지, 자기소개 | 모바일 세로 배치, 넓은 화면에서는 이미지와 글 병렬 배치 |
| Skills | 기술 이름과 간단한 활용 설명 | 의미 있는 목록으로 표시 |
| Projects | GitHub 저장소 카드, 요청 상태 안내 | 저장소 방문, 오류 발생 시 재시도 |
| Contact | 이름, 이메일, 메시지, 제출 버튼 | 입력 검증과 필드별 오류 안내 |
| Footer | 저작권, GitHub 등 소셜 링크 | 외부 프로필로 이동 |
| 스크롤 탑 버튼 | 위로 이동 버튼 | 300px 이상 스크롤하면 표시 |

페이지는 `header`, `nav`, `main`, `section`, `article`, `footer`로 구조화한다. 페이지 대표 제목은 `h1`, 섹션 제목은 `h2`, 프로젝트 카드 제목은 `h3`로 구성한다. 각 주요 섹션에는 앵커 이동용 고유 ID를 부여한다.

### 반응형 기준

- **768px 미만:** 햄버거 메뉴, 세로 중심 배치, 좁은 화면에서도 입력창과 카드가 화면 밖으로 넘치지 않도록 구성한다.
- **768px 이상:** 일반 네비게이션을 표시하고 About과 카드 배치를 확장한다.
- **1024px 이상:** 최대 콘텐츠 폭을 제한하고 넓은 여백과 여러 열의 카드를 사용한다.
- Projects는 `repeat(auto-fit, minmax(...))`를 사용한다. 최소 카드 폭이 화면 폭을 초과하지 않도록 설정한다.
- 버튼과 카드에는 `hover`, `transition`, 카드에는 `box-shadow`를 적용한다. 키보드 사용자를 위한 `:focus-visible` 표시도 제공한다.

## 4. 파일 구성

```text
B1-1/
├── subject.md             # 원본 과제 요구사항
├── PLAN.md                # 수행 기획서
├── README.md              # 실행, 설계 설명, 검증 결과, 제출 정보
├── index.html             # 시맨틱 구조와 정적 콘텐츠
├── css/
│   └── style.css          # 변수, 테마, 레이아웃, 반응형, 애니메이션
├── js/
│   ├── config.js          # GitHub 사용자명 등 공개 설정
│   ├── main.js            # 메뉴, 스크롤, 테마, 초기화
│   ├── projects.js        # API 요청과 프로젝트 상태 렌더링
│   └── contact.js         # 입력 상태, 검증, 제출 결과
└── images/
    ├── profile.*          # 실제 사용할 프로필 이미지
    └── screenshots/       # 제출용 화면 캡처
```

외부 CSS를 연결하고 JavaScript는 `defer` 속성으로 설정 파일부터 순서대로 연결한다. 각 기능 파일의 내부 변수는 함수 범위로 묶어 이름 충돌을 줄인다. 빌드 도구 없이 VS Code와 Live Server로 실행한다. 배포 하위 경로에서도 동작하도록 자산 경로는 상대 경로를 사용한다.

## 5. 상태와 인터랙션 설계

| 기능 | 이벤트 | 상태 | 화면 갱신 |
| --- | --- | --- | --- |
| 다크 모드 | 테마 버튼 `click` | `theme: light / dark` | `data-theme`, 버튼 설명, localStorage 갱신 |
| 모바일 메뉴 | 메뉴 버튼 `click` | `isMenuOpen` | `classList.toggle('active')`, `aria-expanded` 갱신 |
| 프로젝트 조회 | 최초 진입, 재시도 `click` | `status`, `repos`, `error` | 로딩 / 성공 / 오류 / 빈 화면 렌더링 |
| 문의 폼 | `input`, `submit` | 입력값, 필드별 오류, 제출 결과 | 오류 문구와 성공 안내 갱신 |
| 스크롤 UI | `scroll` | 현재 스크롤 위치 | 네비게이션 배경, 스크롤 탑 버튼 표시 변경 |

### 테마

초기 로드 시 localStorage의 유효한 테마 값을 읽고, 값이 없으면 라이트 모드를 사용한다. 버튼을 누르면 상태를 변경한 뒤 `document.documentElement`의 `data-theme` 속성과 저장 값을 갱신한다. 저장소 접근이 실패하더라도 현재 페이지의 테마 전환은 동작하게 한다.

### 네비게이션과 스크롤

- 모바일 메뉴는 버튼으로 열고 닫으며, 메뉴 링크를 선택하면 닫는다. Escape 키로도 닫을 수 있게 한다.
- 앵커 링크는 CSS의 `scroll-behavior: smooth`로 부드럽게 이동하고, 고정 헤더 높이는 `scroll-margin-top`으로 보정한다.
- 스크롤 위치 **60px 이상**에서 네비게이션 배경을 변경하고, **300px 이상**에서 스크롤 탑 버튼을 표시한다. 초기 로드 시에도 위치를 반영한다.
- 스크롤 탑 버튼은 맨 위로 부드럽게 이동한다.
- Intersection Observer의 `threshold`는 **0.2**로 설정하여 화면에 들어온 요소에 표시 클래스를 추가한다.
- `prefers-reduced-motion` 설정에서는 부드러운 이동과 등장 효과를 줄인다. 애니메이션 초기화가 실패해도 본문은 읽을 수 있어야 한다.

DOM 선택에는 `querySelector`와 `querySelectorAll`을 사용한다. 메뉴와 스크롤 효과에서 `classList.add`, `remove`, `toggle`을 활용하고 여러 요소의 이벤트 연결에는 `forEach`를 사용한다.

## 6. GitHub Projects 설계

과제에서 지정한 `https://api.github.com/users/{사용자명}/repos`를 기반으로 공개 저장소를 조회한다. 첫 구현은 최근 수정 순으로 최대 100개를 요청하고 페이지에는 최대 6개를 표시한다. 전체 저장소는 GitHub 프로필 링크로 확인하게 한다. 이 표시 범위는 README에 기록한다.

1. 요청을 시작하면 상태를 `loading`으로 바꾸고 로딩 안내를 렌더링한다.
2. `fetch`와 `async/await`로 요청한다. 중복 요청 중에는 재시도 버튼을 비활성화한다.
3. `response.ok`를 확인하고 HTTP 오류는 `catch`에서 처리할 수 있도록 예외를 발생시킨다.
4. 정상 배열을 받으면 `repos`를 저장하고, 길이에 따라 `success` 또는 `empty`로 전환한다.
5. 네트워크 오류, 잘못된 응답, HTTP 오류 발생 시 `error` 상태와 재시도 버튼을 표시한다.
6. 재시도 버튼은 같은 조회 함수를 호출한다.

| 상태 | 화면 표현 |
| --- | --- |
| loading | ‘프로젝트를 불러오는 중입니다’ 안내 |
| success | 저장소 이름, 설명, 주요 언어, 스타 수, GitHub 링크 카드 |
| error | ‘프로젝트를 불러올 수 없습니다’ + 재시도 버튼 |
| empty | ‘표시할 프로젝트가 없습니다’ 안내 |

설명이나 언어가 없으면 각각 ‘설명이 없습니다’, ‘미분류’로 표시한다. 기본 구현에서는 fork도 포함하고 필터링은 보너스 단계로 둔다. 403 응답도 오류 UI로 처리하며, 레이트 리밋으로 확인된 경우에는 잠시 후 다시 시도하라는 안내를 덧붙인다. 공개 코드에 인증 토큰을 넣지 않는다.

구조분해 할당으로 필요한 필드를 추출하고 `map`과 템플릿 리터럴로 카드 HTML을 생성한다. `innerHTML`에 들어갈 외부 문자열은 HTML 이스케이프 처리하고, 링크는 허용된 HTTPS GitHub 주소인지 확인한다. 상태 문구와 폼 안내는 `textContent`로 갱신한다.

## 7. 문의 폼 설계

이름, 이메일, 메시지에 `label`과 고유 `id`를 연결하고 필수 입력을 표시한다. `submit`에서 `event.preventDefault()`를 호출한 뒤 공백을 제거한 입력값을 검증한다.

- 이름과 메시지: 공백만 입력한 값도 빈 값으로 처리한다.
- 이메일: 필수값과 이메일 형식을 검증한다. 복잡한 정규식보다 `type="email"`의 유효성 기능을 활용한다.
- 오류는 각 필드 아래에 표시하고 `aria-invalid`, `aria-describedby`로 연결한다.
- 제출 실패 시 첫 오류 필드로 포커스를 이동한다.
- `input` 이벤트에서는 이미 오류가 표시된 필드를 재검증하고 성공 안내를 정리한다.
- 전체 검증 통과 시 ‘입력 확인이 완료되었습니다. 이 데모에서는 메시지를 실제 전송하지 않습니다’라고 안내한다.

기본 과제는 프런트엔드 검증까지 수행한다. 실제 전송 기능은 선택 과제로 분리하여 전송하지 않은 메시지가 발송된 것처럼 보이지 않게 한다. 요청 상태와 제출 결과에는 `aria-live`를 사용한다.

## 8. 단계별 수행 계획

| 단계 | 수행 내용 | 완료 기준 |
| --- | --- | --- |
| 1. 준비와 골격 | 개인 콘텐츠 준비, 폴더 생성, Live Server 실행, HTML 구조 작성 | 6개 필수 섹션과 앵커, CSS/JS 연결 확인 |
| 2. 기본 스타일 | CSS 변수, 모바일 레이아웃, Flexbox/Grid, 반응형과 다크 테마 작성 | 모바일·태블릿·데스크톱에서 가로 넘침 없이 표시 |
| 3. 기본 인터랙션 | 메뉴, 테마 저장, 스크롤 이동, 헤더 변경, 탑 버튼, 등장 효과 | 각 이벤트와 화면 변화, 새로고침 후 테마 유지 확인 |
| 4. API 연동 | GitHub 요청, 카드 렌더링, 4가지 상태와 재시도 구현 | 성공·로딩·오류·빈 상태를 각각 재현 |
| 5. 폼 UX | 입력 상태, 검증 함수, 필드 오류, 제출 결과 구현 | 빈 값·잘못된 이메일 차단, 정상 입력 성공 안내 |
| 6. 통합 검증 | 반응형, 키보드 조작, 콘솔 오류, 제약사항 점검 | 아래 검증표를 통과하고 결함 수정 |
| 7. 배포와 제출 | GitHub Pages 설정, 배포 URL 확인, 스크린샷, README 작성 | 외부 접속 가능한 사이트와 제출 자료 완성 |

각 단계는 동작하는 단위로 커밋한다. 화면 구현과 API 연동을 한 번에 진행하기보다, 정적 레이아웃이 완성된 뒤 동작을 붙여 문제 원인을 구분한다.

## 9. 검증 계획

최신 Chrome과 개발자 도구를 사용한다. 최소 375px, 768px, 1024px, 1440px 폭을 확인하고, 메뉴 전환 경계인 767/768px도 비교한다.

| 검증 항목 | 방법 | 통과 기준 |
| --- | --- | --- |
| 구조와 접근성 | HTML 및 키보드 탐색 확인 | 시맨틱 태그, 제목 순서, 이미지 alt, label 연결, 포커스 표시 정상 |
| 반응형 | 화면 폭 변경 | 가로 넘침 없음, 메뉴 전환 정상, 카드와 폼 읽기 가능 |
| 메뉴 | 클릭, 링크 선택, Escape, 화면 확대 | 상태와 표시 일치, 닫기와 섹션 이동 정상 |
| 테마 | 전환 후 새로고침 | 테마 유지, 두 테마에서 본문과 오류 문구 식별 가능 |
| 스크롤 | 60px·300px 전후 이동 | 헤더와 탑 버튼 표시 조건 충족, 상단 이동 정상 |
| 등장 효과 | 스크롤 및 동작 감소 설정 확인 | 요소 표시 정상, 동작 감소 설정 반영 |
| API 로딩·성공 | Network 속도 제한 후 조회 | 로딩 안내 후 실제 저장소 카드 표시 |
| API 오류·재시도 | 오프라인, 존재하지 않는 사용자명, 테스트용 403 응답 | 오류와 재시도 표시, 네트워크 복구 후 정상 전환 |
| API 빈 상태 | 테스트 응답 `[]` 사용 | 빈 상태 문구 표시 |
| 폼 | 빈 값, 공백, 이메일 오류, 정상 입력 제출 | 필드 오류와 성공 안내가 조건에 맞게 표시 |
| 배포 | 실제 Pages URL에서 다시 확인 | CSS·JS·이미지 로드, API·테마·메뉴·폼 정상 |

403과 빈 배열은 실제 호출 제한을 소모하기보다 개발용 응답 대체로 재현한다. 테스트 후 원래 요청으로 복구한다. 저장소 설명에 HTML 특수문자가 포함된 경우도 점검한다. 검증 결과와 미해결 사항은 README에 기록하며, 핵심 요구사항 미충족 상태로 완료 처리하지 않는다.

## 10. README와 최종 제출물

README에는 다음 내용을 작성한다.

- 프로젝트 소개, 사용 기술, 로컬 실행 방법.
- GitHub 저장소 URL과 실제 GitHub Pages 배포 URL.
- 데스크톱, 모바일, 다크 모드 스크린샷.
- 폴더 구조와 필수 기능 목록.
- 다크 모드, API, 폼의 이벤트 → 상태 → 렌더링 흐름 설명.
- Flexbox와 Grid 선택 이유, 시맨틱 태그 설계 기준.
- 스크롤 기준값 60px/300px, Observer 임계값 0.2.
- 프로젝트 표시 범위, API 요청 제한, 문의 폼의 실제 전송 여부.
- 주요 검증 결과와 알려진 제한사항.

최종 제출 전에는 임시 콘텐츠 제거, 실제 GitHub 사용자명 적용, 배포 URL 접속, 콘솔 오류 유무, 제출 스크린샷과 최종 화면의 일치를 확인한다.

## 11. 선택 기능 우선순위

필수 기능과 배포 검증이 끝난 경우에만 진행한다.

1. **언어별 프로젝트 필터:** `filter`와 필터 상태를 추가해 상태 기반 렌더링 학습을 확장한다. 해당 언어의 결과가 없으면 빈 상태를 표시한다.
2. **시스템 다크 모드 감지:** 사용자가 저장한 설정이 없을 때만 시스템 테마를 초기값으로 사용한다.
3. **Hero 타이핑 효과:** 읽기와 접근성을 해치지 않는 범위에서 추가한다.
4. **폼 실제 전송:** 서비스 설정이 필요한 별도 확장으로 수행하고 전송 중·성공·실패 상태를 추가한다.

필수 기능 구현과 오류 상태 검증, README, 배포를 선택 기능보다 우선한다.
